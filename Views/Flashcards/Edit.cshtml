@model FlashcardApp_vs.Models.Flashcard

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Flashcard</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="FlashcardQuestion" class="control-label"></label>
                <input asp-for="FlashcardQuestion" class="form-control" />
                <span asp-validation-for="FlashcardQuestion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FlashcardAnswer" class="control-label"></label>
                <input asp-for="FlashcardAnswer" class="form-control" />
                <span asp-validation-for="FlashcardAnswer" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
